{
  "name": "deploy",
  "version": "1.0.0",
  "description": "got.bet deployment",
  "engines": {
    "node": ">=10.15"
  },
  "scripts": {
    "start": "webpack --config ./webpack.dev.js --watch & nodemon -L --watch dev --watch config dev/index.js & wait",
    "copy:config": "cp package.json dist && cp yarn.lock dist && cp -r config dist",
    "build": "rm -rf dist/* && webpack --config ./webpack.prod.js && yarn copy:config"
  },
  "author": "Nazar Aziz <mcnazar@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "shipit-cli": "^1.3.0",
    "shipit-deploy": "2.0.0",
    "shipit-utils": "1.3.0"
  }
}
